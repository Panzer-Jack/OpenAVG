export enum StageType {
  GLOBAL = 'GLOBAL',
  NOVEL = 'NOVEL',
  GAME_2D = 'GAME_2D',
  GAME_3D = 'GAME_3D',
}

export enum ApiEnum {
  fetchGlobalConfig = 'fetchGlobalConfig',
  fetchChapter = 'fetchChapter',
}
